<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dzteamdev.flexyp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Dashboard.HomeActivity" />
        <activity android:name=".Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main.LoginActivity" />
        <activity android:name=".Main.RegisterActivity" />
        <activity android:name=".Dashboard.SettingsActivity" />
        <activity android:name=".Dashboard.CartActivity" />
        <activity android:name=".Dashboard.Mobile.OffersActivity" />
        <activity android:name=".Dashboard.OfferDetailsActivity" />
        <activity android:name=".Admins.HomeAdminActivity" />
        <activity android:name=".Dashboard.MyRequestActivity" />
        <activity android:name=".Dashboard.BeinSport.BSOffresDetailsActivity"></activity>

        <service android:name=".Service.ListenRecharge" />
    </application>

</manifest>